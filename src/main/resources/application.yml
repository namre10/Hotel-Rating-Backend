server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotelRating_Microservice?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver   # optional but safe

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect   # optional but helps
